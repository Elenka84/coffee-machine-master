
Уберу WhatsApp и Telegram из интерфейса (кнопки, ссылки, иконки) на лендинге и в калькуляторе. Останется только телефон.

### Файлы
- `src/pages/Index.tsx` — удалить кнопки/ссылки WhatsApp и Telegram в hero, секции контактов и футере; оставить только телефон.
- `src/pages/Calculator.tsx` — удалить кнопки WhatsApp/Telegram в блоке контактов, оставить телефон.

### Что НЕ трогаю
- Поля `whatsapp` и `telegram` в `site_settings` и в админке (`Settings.tsx`) — оставляю в БД и в форме настроек, чтобы при необходимости можно было быстро вернуть. Просто не отображаю на публичной части.

Если нужно полностью удалить и из админки/БД — скажите, сделаю отдельной миграцией.
